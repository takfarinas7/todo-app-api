# todo-app-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: etudiant23-ns # VOTRE NAMESPACE UNIQUE
spec:
  selector:
    app: todo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort